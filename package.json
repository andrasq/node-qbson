{
  "name": "qbson",
  "version": "0.0.13",
  "description": "fast js-only bson encode / decode",
  "license": "Apache-2.0",
  "main": "qbson.js",

  "author": "andrasq",
  "repository": { "type": "git", "url": "git://github.com/andrasq/node-qbson" },
  "keywords": ["fast", "mongo", "mongodb", "bson", "buffalo", "Andras"],
  "readme": "see Readme.md in the repo",

  "dependencies": {
    "ieee-float": "0.6",
    "qtimeout": "0.9.3"
  },
  "devDependencies": {
    "bson": "1.0.4"
  },

  "scripts": {
    "test": "for f in test-*.js ; do echo $f: && qnit $f || return 2; done",
    "coverage": "nyc --reporter text --reporter lcov npm test",
    "clean": "rm -rf .nyc_output coverage"
  }
}
